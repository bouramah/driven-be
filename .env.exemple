# Configuration de l'application Flask
FLASK_APP=run.py
FLASK_ENV=development
FLASK_DEBUG=True

# Clés secrètes
SECRET_KEY=votre_cle_secrete_tres_complexe_a_changer_en_production
JWT_SECRET_KEY=votre_cle_jwt_secrete_tres_complexe_a_changer_en_production

# Configuration de la base de données
# Développement (SQLite)
DATABASE_URL=sqlite:///app.db

# Test (MySQL)
DB_USER_TEST=root
DB_PASSWORD_TEST=root
DB_HOST_TEST=10.173.86.106
DB_NAME_TEST=driven_test
DATABASE_URL_TEST=mysql+pymysql://${DB_USER_TEST}:${DB_PASSWORD_TEST}@${DB_HOST_TEST}/${DB_NAME_TEST}

# Production (MySQL)
DB_USER_PROD=root
DB_PASSWORD_PROD=root
DB_HOST_PROD=10.173.86.106
DB_NAME_PROD=driven
DATABASE_URL_PROD=mysql+pymysql://${DB_USER_PROD}:${DB_PASSWORD_PROD}@${DB_HOST_PROD}/${DB_NAME_PROD}

# Configuration JWT
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=604800

# Configuration de journalisation
LOG_LEVEL=DEBUG
LOG_FILE=app.log

# Configuration du serveur
HOST=0.0.0.0
PORT=5001

# Configuration de sécurité
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
